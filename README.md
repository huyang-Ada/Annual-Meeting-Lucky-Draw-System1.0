# 🎉 年会抽奖系统

一个简单易用的年会抽奖网页应用，支持1-10等奖的抽奖功能，适合公司年会、团建活动等场景使用。

## ✨ 功能特点

- 🎯 支持1-10等奖，共10个奖项等级
- 🎲 真随机抽奖算法，公平公正
- 📊 实时显示中奖名单和剩余人数
- 💾 支持导入自定义人员名单（JSON格式）
- 📤 支持导出抽奖结果
- 🎨 精美的动画效果和渐变背景
- 📱 响应式设计，支持各种屏幕尺寸
- 🚀 单文件部署，无需后端服务器

## 🎁 奖项设置

| 奖项 | 人数 |
|------|------|
| 一等奖 | 2人 |
| 二等奖 | 3人 |
| 三等奖 | 4人 |
| 四等奖 | 5人 |
| 五等奖 | 6人 |
| 六等奖 | 7人 |
| 七等奖 | 8人 |
| 八等奖 | 9人 |
| 九等奖 | 10人 |
| 十等奖 | 20人 |

**总计：74人**

## 🚀 快速开始

### 方法一：直接使用

1. 下载 `lottery.html` 文件
2. 双击打开文件，即可在浏览器中使用
3. 系统默认包含100人的示例名单（员工001-员工100）

### 方法二：自定义人员名单

1. 准备人员名单JSON文件（参考下方格式）
2. 打开 `lottery.html`
3. 点击"导入人员名单"按钮，选择你的JSON文件
4. 开始抽奖

### 人员名单格式

创建一个 `participants.json` 文件，格式如下：

```json
[
  "张三",
  "李四",
  "王五",
  "赵六",
  "..."
]
```

**注意：** 必须是JSON数组格式，每个元素是一个字符串（人员姓名）。

## 📖 使用说明

### 1. 选择奖项
点击对应的奖项按钮（如"一等奖 (2人)"），按钮会高亮显示。

### 2. 开始抽奖
点击"开始抽奖"按钮，屏幕上会快速滚动显示随机名字。

### 3. 停止抽奖
再次点击"停止"按钮，系统会随机选出获奖者并显示在屏幕上。

### 4. 查看结果
所有中奖名单会实时显示在页面下方的"中奖名单"区域。

### 5. 导出结果
抽奖完成后，点击"导出结果"按钮，可以下载包含所有中奖信息的JSON文件。

### 6. 重置抽奖
如需重新开始，点击"重置抽奖"按钮，所有数据将被清空。

## 🎮 操作流程

```
导入人员名单（可选）
    ↓
选择奖项（如：一等奖）
    ↓
点击"开始抽奖"
    ↓
点击"停止"确定获奖者
    ↓
继续抽取其他奖项
    ↓
导出结果（可选）
```

## 💡 使用技巧

1. **建议抽奖顺序**：从一等奖开始抽，逐级往下，营造悬念感
2. **大屏幕展示**：使用投影仪或大屏幕展示，效果更佳
3. **全屏模式**：按 `F11` 进入全屏模式，获得更好的视觉效果
4. **音效配合**：可以配合背景音乐使用，增强现场气氛
5. **备份结果**：抽奖完成后及时导出结果，避免误操作丢失数据

## 🔧 技术栈

- 纯HTML5 + CSS3 + JavaScript
- 无任何第三方依赖
- 无需Node.js或其他运行环境
- 无需后端服务器

## 📂 文件说明

```
.
├── lottery.html          # 抽奖系统主文件（包含所有代码）
├── README.md            # 说明文档
└── participants.json    # 人员名单示例（可选）
```

## 🌐 部署到GitHub Pages

1. 创建GitHub仓库
2. 上传 `lottery.html` 和 `README.md`
3. 进入仓库设置 → Pages
4. 选择分支（通常是main）
5. 保存后等待几分钟
6. 访问 `https://你的用户名.github.io/仓库名/lottery.html`

## 🎨 自定义修改

### 修改奖项设置

编辑 `lottery.html` 文件，找到 `prizeConfig` 数组：

```javascript
const prizeConfig = [
    { level: 1, name: '一等奖', count: 2, color: '#ff4757' },
    // 修改 count 可以改变人数
    // 修改 name 可以改变奖项名称
    // 修改 color 可以改变颜色
];
```

### 修改默认人员

编辑 `lottery.html` 文件，找到 `allParticipants` 数组：

```javascript
let allParticipants = [];
for (let i = 1; i <= 100; i++) {
    allParticipants.push(`员工${i.toString().padStart(3, '0')}`);
}
// 可以直接替换为：
// let allParticipants = ["张三", "李四", "王五", ...];
```

## ❓ 常见问题

**Q: 可以重复中奖吗？**
A: 不可以。每个人只能中奖一次，中奖后会从候选名单中移除。

**Q: 抽奖结果可以撤销吗？**
A: 单个奖项无法撤销，但可以点击"重置抽奖"重新开始。

**Q: 支持多少人参与？**
A: 理论上无限制，但建议不超过10000人以保证性能。

**Q: 可以在手机上使用吗？**
A: 可以，但建议使用电脑或平板以获得更好的展示效果。

**Q: 导出的结果是什么格式？**
A: JSON格式，包含时间戳、参与人数和各奖项中奖名单。

## 📝 更新日志

### v1.0.0 (2026-02-12)
- ✨ 初始版本发布
- 🎯 支持1-10等奖抽奖
- 📊 实时显示中奖名单
- 💾 支持导入/导出功能
- 🎨 精美的UI设计和动画效果

## 📄 许可证

MIT License - 可自由使用、修改和分发

## 🤝 贡献

欢迎提交Issue和Pull Request！

## 📧 联系方式

如有问题或建议，欢迎通过GitHub Issues反馈。

---

⭐ 如果这个项目对你有帮助，欢迎给个Star！

🎊 祝你的年会圆满成功！
